//! Native headless benchmark and golden-trajectory generator (F8.1).
//!
//! Section 01 lands the binary so the workspace shape is real; the benchmark
//! and the golden-trajectory writer arrive with the physics (sections 02+).

fn main() {
    println!(
        "sailgym-bench {} — no simulation to run yet (section 01 skeleton)",
        env!("CARGO_PKG_VERSION")
    );
}
